namespace idesyde.core.headers;

table LabelledPortedArc {
    src: string;
    dst: string;
    src_port: string;
    dst_port: string;
    label: string;
}

table DecisionModelHeader {
    category: string;
    covered_elements: [string];
    covered_relations: [LabelledPortedArc];
    model_body_paths: [string];
}

table DesignModelHeader {
    category: string;
    elements: [string];
    relations: [LabelledPortedArc];
    model_paths: [string];
}
