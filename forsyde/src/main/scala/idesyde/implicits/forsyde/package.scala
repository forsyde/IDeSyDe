package idesyde.implicits

import spire.math.Rational

package object forsyde {
  // given scala.math.Fractional[Rational] = spire.compat.fractional[Rational]
  // given scala.math.Ordering[Rational] = spire.compat.ordering[Rational]
  // given Conversion[Double, Rational] = (d: Double) => Rational(d)
  // given Conversion[Int, Rational] = (i: Int) => Rational(i)

}
